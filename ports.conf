# Time Tracking App - Port Configuration
# Modify these values to change the ports used by the application

# Frontend port (default: 5173)
FRONTEND_PORT=5173

# Backend port (default: 8000)
BACKEND_PORT=8000
